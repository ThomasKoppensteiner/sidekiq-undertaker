require:
  - rubocop-rake
  - rubocop-rspec
  - rt_rubocop_defaults

inherit_from: .rubocop_todo.yml

AllCops:
  TargetRubyVersion: 2.6

Layout/LineLength:
  Exclude:
    - 'sidekiq-undertaker.gemspec'
